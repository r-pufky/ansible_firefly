---
- name: 'Converge'
  hosts: 'all'
  gather_facts: false
  tasks:
    - name: 'Converge | apply r_pufky.srv.firefly'
      ansible.builtin.include_role:
        name: 'r_pufky.srv.firefly'

- name: 'Converge'
  hosts: 'firefly-debian-13-backup_remove'
  gather_facts: false
  tasks:
    - name: 'Converge | apply r_pufky.srv.firefly (remove backups)'
      ansible.builtin.include_role:
        name: 'r_pufky.srv.firefly'
      vars:
        firefly_srv_backup_enable: false
